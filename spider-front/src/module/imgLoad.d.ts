/**
 * ImageLoad
 */
interface checkBoxAble {
  checksList: string[],
  checkedList: string[],
  checkAll: boolean
}
interface pagesConfAble {
  currentPage: number,
  pageSize: number
}

type inputType = HTMLInputElement
